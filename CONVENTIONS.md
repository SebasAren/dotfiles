# Development Conventions

## Code Style

### Lua
- 2-space indentation
- Format with [StyLua](https://github.com/JohnnyMorganz/StyLua) (`mise run stylua`)
- Lint with [luacheck](https://github.com/mpeterv/luacheck) (`mise run luacheck`)
- Use `---@type` annotations for LuaLS
- Use `snake_case` for functions and variables
- Prefer `require().setup()` over `vim.cmd()` for plugin configuration

### Python
- 4-space indentation
- Format and lint with [ruff](https://github.com/astral-sh/ruff) (`mise run ruff`)
- `snake_case` for functions and variables
- `PascalCase` for classes

### Shell
- Use `#!/usr/bin/env bash` and `set -euo pipefail`
- Lint with [shellcheck](https://github.com/koalaman/shellcheck) (`mise run shellcheck`)
- Lowercase filenames with hyphens for multi-word names
- One concern per file (see `.bashrc.d/` structure)

### TypeScript/JavaScript
- Lint and typecheck with `mise run npx tsc`
- Format with prettier (via `mise run bun` or prettierd in Neovim)
- Avoid `as any` and `@ts-ignore`

## Git Workflow

- **Conventional commits**: `<type>(<scope>): <description>`
  - Types: `feat`, `fix`, `docs`, `style`, `refactor`, `perf`, `test`, `build`, `ci`, `chore`, `revert`
  - Example: `feat(nvim): add rust-analyzer LSP config`
- **Atomic commits**: one logical change per commit
- **Pre-commit hooks**: fast format checks (StyLua, YAML/JSON validation, whitespace trimming)
- **Commit messages**: optionally generated by pi via worktrunk

## Dotfile Management

- Use GNU Stow for all configuration symlinks
- Each tool gets its own top-level directory
- Config files go in their standard XDG location within the tool directory: `tool-name/.config/tool/`
- Edit files in the repo, never in the symlink target
- Avoid hard-coded paths that assume a specific username or machine

## Docker Services

- Each service has its own directory under `docker/docker-services/`
- One `docker-compose.yml` or `.yaml` per service (both extensions are in use — don't try to normalize)
- Use environment variables for secrets and user-specific config
- `.env` files are gitignored
- Media mounts from `/var/stash/media` and `/var/stash2/media` (read-only where possible)
- Use `:U,z` SELinux labels for writable volumes, `:ro,z` or `:ro` for read-only mounts
- Use UID/GID 1000:1000 when user mapping is needed
- Include health checks for critical services
- Services behind nginx-proxy-manager share the external `nginx` Docker network

## Neovim

- Plugin specs in `lua/plugins/` — each file returns a Lazy.nvim table
- LSP server configs in `lsp/*.lua` — one file per server
- Use `pcall(require, "custom-settings")` for machine-specific overrides (file is gitignored)
- Keybinding conventions: `<space>` as leader, `gd` for definition, `gr` for references
- Never use `vim.cmd("packadd")` — Lazy.nvim handles loading

## Secrets

- Never commit secrets or credentials to the repo
- Use `~/.secrets.tpl` with Proton Pass CLI template syntax: `{{ pass://path/to/key }}`
- Secrets are resolved lazily on first use, not at shell startup
- API keys needed: `EXA_API_KEY`, `CONTEXT7_API_KEY` (for pi extensions)

## File Organization

```
.
├── [tool]/.config/[tool]/     # XDG-compliant config location
├── docker/docker-services/    # One dir per service
├── scripts/hooks/             # Git hooks
├── bashrc/.bashrc.d/          # Modular shell scripts
├── mise.toml                  # Tool versions
├── AGENTS.md                  # Agent-facing guide
├── CONVENTIONS.md             # This file
└── README.md                  # Human-facing setup guide
```

## Pre-commit Checks

Before committing, the pre-commit hook runs:
1. StyLua on `.lua` files
2. YAML/JSON validation
3. Whitespace trimming (trailing whitespace, missing final newline)

Install hooks: `git config core.hooksPath .githooks`
