---
globs:
  - "*.py"
description: Python coding standards
---

- Use 4-space indentation
- `snake_case` for functions and variables, `PascalCase` for classes
- Use `ruff` for linting, `black` + `isort` for formatting
